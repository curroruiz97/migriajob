/**
 * @deprecated — La app ahora usa Supabase JS client (vía REST + RLS).
 * Este archivo se conserva como stub vacío para que `drizzle-kit` siga
 * funcionando para generar migraciones, pero NO se usa en runtime.
 *
 * Para queries en server components/actions usa createClient de @/lib/supabase/server.
 */

export * from './schema';
